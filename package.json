{
  "name": "tiptap-collaboration-mcp",
  "version": "1.1.2",
  "main": "build/index.js",
  "type": "module",
  "scripts": {
    "build": "npm run clean && tsc -p tsconfig.build.json && chmod 755 build/index.js",
    "start": "npm run build && node build/index.js",
    "dev": "tsx src/index.ts",
    "clean": "rm -rf build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "vitest --run",
    "test:live": "vitest --run tests/live",
    "test:watch": "vitest --watch",
    "inspect": "npx @modelcontextprotocol/inspector tsx src/index.ts"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "description": "Connects to a Tiptap Collaboration server",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.2",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@modelcontextprotocol/inspector": "^0.16.5",
    "@types/node": "^24.2.1",
    "prettier": "^3.9.6",
    "tsx": "^4.20.4",
    "typescript": "^5.9.2",
    "vitest": "^2.1.9"
  },
  "bin": {
    "tiptap-collaboration-mcp": "./build/index.js"
  },
  "files": [
    "build"
  ]
}
